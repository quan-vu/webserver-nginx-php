# Dockerizing PHP application with Nginx and PHP7-FPM

This is a simple deployment for a PHP application using Docker.

## How it works

Start docker compose and build image:

```shell
$ docker-compose up --build
```
